android {
    compileSdkVersion 33  // Use latest version
    defaultConfig {
        minSdkVersion 21  // Update if lower
        targetSdkVersion 33
    }
}
classpath 'com.android.tools.build:gradle:7.3.0'
