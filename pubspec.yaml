name: espo_management
description: A social media app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.3
  http: ^0.13.6
  firebase_core: ^2.15.0
  firebase_auth: ^4.6.2
  cloud_firestore: ^4.8.2
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^15.1.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
